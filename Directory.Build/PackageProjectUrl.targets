<Project>
  <Target Name="GetPackageProjectUrl" AfterTargets="GitInfo">
    <PropertyGroup>
      <PackageProjectUrl>https://justinwritescode.com/$([System.IO.Path]::GetFileNameWithoutExtension('$(GitRepositoryUrl)'))</PackageProjectUrl>
    </PropertyGroup>
  </Target>
</Project>
